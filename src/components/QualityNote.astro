---
interface Props {
  note?: string;
}

const { note } = Astro.props;
---

{
  note ? (
    <p class="quality-note">
      <span class="quality-label">收录理由</span>
      {note}
    </p>
  ) : null
}
